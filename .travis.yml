arch: s390x
services:
  - docker
env:
  global:
    - BUILD_DIR="$PWD/stable/slim"
    - REPO="quay.io/ibmz/debian"
    - TAG="$REPO:stable-slim"
before_script: 
  - docker login quay.io -u $ROBOT -p $ROBOT_TOKEN
script: 
  - cd $BUILD_DIR && docker build . --tag $TAG
after_script:
  - docker push $REPO
