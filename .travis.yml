arch: s390x
services:
  - docker
env:
  global:
    - BUILD_DIR="$PWD/bullseye"
    - REPO="quay.io/ibm/debian"
    - TAG="$REPO:bullseye"
before_script: 
  - docker login quay.io -u $ROBOT -p $ROBOT_TOKEN
script: 
  - cd $BUILD_DIR && docker build . --tag $TAG
after_script:
  - docker push $REPO
